name: Otterkit COBOL Build & Test

on:
  push:
    branches: [ main ]
  pull_request:

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: testdb
        ports:
          - 5432:5432

    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Setup .NET 7
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '7.0.x'

      - name: Setup .NET 8
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Install Otterkit
        run: |
          dotnet tool install --global Otterkit --version 1.0.50-alpha
          echo "$HOME/.dotnet/tools" >> $GITHUB_PATH

      - name: Verify Otterkit
        run: otterkit --version

      - name: Create .NET project
        run: |
          dotnet new console -o cobolapp
          cd cobolapp
          dotnet add package Npgsql --version 8.0.3

      - name: Copy COBOL source
        run: |
          mkdir -p cobolapp/src
          cp src/main.cob cobolapp/src/main.cob
          cp src/EmployeeRepo.cob cobolapp/src/EmployeeRepo.cob
          ls -la cobolapp/src/

      - name: Setup database schema
        env:
          PGPASSWORD: postgres
        run: |
          psql -h localhost -U postgres -d testdb -c "
            CREATE TABLE IF NOT EXISTS emp (
              id   SERIAL PRIMARY KEY,
              name VARCHAR(100),
              age  INT
            );"

      - name: Compile COBOL program
        run: |
          otterkit build cobolapp/src/main.cob cobolapp/src/EmployeeRepo.cob -o cobolapp/bin/Debug/net8.0/Program.dll

      - name: Run COBOL program
        working-directory: cobolapp
        env:
          PGHOST: localhost
          PGPORT: 5432
          PGUSER: postgres
          PGPASSWORD: postgres
          PGDATABASE: testdb
        run: dotnet run

      - name: Verify DB
        env:
          PGPASSWORD: postgres
        run: |
          psql -h localhost -U postgres -d testdb -c "SELECT * FROM emp;"
